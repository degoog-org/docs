# Placeholder Assets

Screenshot placeholders are visible in the user docs as styled image figures. After adding screenshots under `assets/images/screenshots/`, replace the placeholder image `src` with the real path recorded in its `data-screenshot-target` attribute.

This directory is kept so placeholder-specific assets can be added without touching the generated page shell.
